[project]
name = "auth-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "yablonovskydmutro1@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
auth-service = "auth_service.main:run"

[build-system]
requires = ["uv_build>=0.11.26,<0.12.0"]
build-backend = "uv_build"
