[project]
name = "academic-monitoring"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.138.2",
    "uvicorn>=0.49.0",
]

[tool.uv.workspace]
members = [
    "import-service",
    "ml-service",
]
